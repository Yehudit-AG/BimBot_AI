-- Migration 001: Initial Schema
-- This migration creates the base schema for BimBot AI Wall

-- This is handled by init.sql for initial setup
-- Future migrations will be added here for schema changes

-- Example future migration structure:
-- ALTER TABLE drawings ADD COLUMN new_field VARCHAR(100);
-- CREATE INDEX idx_drawings_new_field ON drawings(new_field);